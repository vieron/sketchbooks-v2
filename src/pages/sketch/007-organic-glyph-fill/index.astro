---
import SketchLayout from '../../../layouts/SketchLayout.astro';
import { getSketchById } from '../../../data/sketches';
import OrganicGlyphFill from './_OrganicGlyphFill';

const sketch = getSketchById('organic-glyph-fill');
if (!sketch) throw new Error('Missing sketch metadata');
---

<SketchLayout sketch={sketch}>
  <OrganicGlyphFill client:only="react" />
</SketchLayout>
